services:
  kgs_bot:
    build: .
    container_name: kgs_converter_bot
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - ./data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
